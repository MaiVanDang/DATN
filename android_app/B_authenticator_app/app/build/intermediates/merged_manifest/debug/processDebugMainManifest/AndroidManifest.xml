<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.datn.authenticator"
    android:versionCode="1"
    android:versionName="1.0.0-demo" >

    <uses-sdk
        android:minSdkVersion="29"
        android:targetSdkVersion="36" />

    <!--
      Permissions per Bảng 5.1 (BioAuth Authenticator column):
        - BODY_SENSORS         : accelerometer, gyroscope, magnetometer
        - FOREGROUND_SERVICE   : run AuthenticationService while user is away
        - FOREGROUND_SERVICE_DATA_SYNC : Android 14+ subtype
        - POST_NOTIFICATIONS   : show ongoing TRUSTED/WARNING notification
        - SYSTEM_ALERT_WINDOW  : overlay FallbackUI on top of other apps
        - HIGH_SAMPLING_RATE_SENSORS (Android 12+) : ensure 50 Hz available
    -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Hardware requirements -->
    <uses-feature
        android:name="android.hardware.sensor.accelerometer"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.sensor.gyroscope"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.sensor.compass"
        android:required="false" />

    <permission
        android:name="com.datn.authenticator.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.datn.authenticator.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" />

    <application
        android:name="com.datn.authenticator.AuthenticatorApp"
        android:allowBackup="false"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:debuggable="true"
        android:extractNativeLibs="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:testOnly="true"
        android:theme="@style/Theme.BioAuthAuthenticator" >

        <!-- Owner biometric enrollment — app entry point; skips to QuizActivity if already enrolled -->
        <activity
            android:name="com.datn.authenticator.ui.OwnerEnrollmentActivity"
            android:exported="true"
            android:label="@string/enroll_owner_title" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Shake-pattern enrollment — runs between biometric enrollment and quiz -->
        <activity
            android:name="com.datn.authenticator.ui.FallbackEnrollActivity"
            android:exported="false"
            android:label="Đăng ký mẫu lắc" />

        <!-- Quiz screen — shown after enrollment; auth service runs throughout -->
        <activity
            android:name="com.datn.authenticator.ui.QuizActivity"
            android:exported="false"
            android:label="@string/quiz_title" />

        <!--
             Fallback UI: overlay shown when state = UNKNOWN.
             Theme makes it transparent + on top per Mục 5.3.1.
        -->
        <activity
            android:name="com.datn.authenticator.fallback.FallbackActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleInstance"
            android:showOnLockScreen="true"
            android:taskAffinity=""
            android:theme="@style/Theme.Fallback" />

        <!-- Foreground service (Mục 5.3.1, AuthenticationService) -->
        <service
            android:name="com.datn.authenticator.service.AuthenticationService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Boot receiver — auto-start service after reboot (optional) -->
        <receiver
            android:name="com.datn.authenticator.util.BootReceiver"
            android:enabled="true"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="com.datn.authenticator.androidx-startup"
            android:exported="false" >
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.lifecycle.ProcessLifecycleInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                android:value="androidx.startup" />
        </provider>

        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:directBootAware="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.DUMP" >
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.INSTALL_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SKIP_FILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SAVE_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.BENCHMARK_OPERATION" />
            </intent-filter>
        </receiver>
    </application>

</manifest>