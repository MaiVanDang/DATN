<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Sensor + foreground-service permissions:
        BODY_SENSORS                  : accelerometer, gyroscope, magnetometer
        FOREGROUND_SERVICE            : required for AuthenticationService
        FOREGROUND_SERVICE_DATA_SYNC  : Android 14+ subtype
        POST_NOTIFICATIONS            : ongoing status notification (Android 13+)
        SYSTEM_ALERT_WINDOW           : overlay the fallback UI on top of other apps
        HIGH_SAMPLING_RATE_SENSORS    : 50 Hz capture on Android 12+
    -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Hardware requirements -->
    <uses-feature
        android:name="android.hardware.sensor.accelerometer"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.sensor.gyroscope"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.sensor.compass"
        android:required="false" />

    <application
        android:name=".AuthenticatorApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.BioAuthAuthenticator"
        tools:targetApi="34">

        <!-- Mode selection — app entry point. Lets user pick "Walking" vs "All-action" before enrolling. -->
        <activity
            android:name=".ui.ModeSelectActivity"
            android:exported="true"
            android:label="@string/mode_select_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Owner biometric enrollment — reached after ModeSelectActivity; skips to QuizActivity if already enrolled -->
        <activity
            android:name=".ui.OwnerEnrollmentActivity"
            android:exported="false"
            android:label="@string/enroll_owner_title" />

        <!-- Touch behaviour enrollment — guided tap/scroll/keystroke + RF training -->
        <activity
            android:name=".ui.TouchEnrollActivity"
            android:exported="false"
            android:label="Đăng ký hành vi chạm" />

        <!-- Shake-pattern enrollment — runs between touch enrollment and quiz -->
        <activity
            android:name=".ui.FallbackEnrollActivity"
            android:exported="false"
            android:label="Đăng ký mẫu lắc" />

        <!-- Quiz screen — shown after enrollment; auth service runs throughout -->
        <activity
            android:name=".ui.QuizActivity"
            android:exported="false"
            android:label="@string/quiz_title" />

        <!-- Fallback shake-pattern UI shown when state = UNKNOWN.
             Transparent theme + show-on-lock so it can overlay other apps. -->
        <activity
            android:name=".fallback.FallbackActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:showOnLockScreen="true"
            android:taskAffinity=""
            android:theme="@style/Theme.Fallback" />

        <!-- Foreground service running the authentication loop -->
        <service
            android:name=".service.AuthenticationService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Boot receiver — auto-start service after reboot (optional) -->
        <receiver
            android:name=".util.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
